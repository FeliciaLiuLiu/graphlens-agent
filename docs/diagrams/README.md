# Diagram Assets

These files are saved in both `SVG` and `PNG`.

`SVG` is better for editing and resizing.
`PNG` is better for direct copy/paste into documents and slides.

Diagram set:

- `overall-architecture.svg`
- `overall-architecture.png`
- `model-skills-harness-boundary.svg`
- `model-skills-harness-boundary.png`
- `runtime-flow.svg`
- `runtime-flow.png`
- `analyst-question-mapping.svg`
- `analyst-question-mapping.png`
- `project-folders-and-classes.svg`
- `project-folders-and-classes.png`

They are static image files, not Mermaid source, so they do not depend on a renderer.
